# PPO RL mit palaestrAI — Implementierungsplan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Ersetze DummyBrain/DummyMuscle durch PPO-Agenten (Attacker + Defender) im palaestrAI-Framework für adversariales Reinforcement Learning auf dem Soja-Szenario S1.

**Architecture:** `PPONet(MLP 99→128→64)` mit `policy_head(→20 logits)` + `value_head(→1)`. PPOMuscle macht Inferenz, kodiert `(sampled_logits, log_prob, value)` in `additional_data`. PPOBrain sammelt Trajectory pro Episode, führt PPO-Update bei `done=True` aus, sendet `state_dict` zurück. Budget-Constraint: `Softmax(logits) × budget` (Summe = budget garantiert). Beide Agenten (Attacker + Defender) sind symmetrisch gebaut, aber mit unterschiedlichen Rewards und Budgets.

**Tech Stack:** Python 3.9+, PyTorch (neu zu installieren), NumPy, palaestrAI 3.4.1.

---

### Task 0: PyTorch installieren und verifizieren

**Files:** Keine Code-Änderungen.

**Step 1: PyTorch installieren**

```bash
pip install torch --index-url https://download.pytorch.org/whl/cpu
```

**Step 2: Verifikation**

```bash
python -c "import torch; print('PyTorch:', torch.__version__); t = torch.tensor([1.0, 2.0]); print('OK:', t.softmax(0))"
```

Erwartete Ausgabe:
```
PyTorch: 2.x.x+cpu
OK: tensor([0.2689, 0.7311])
```

---

### Task 1: `provider_sim/rl/__init__.py` + `network.py` — PPONet

**Files:**
- Create: `provider_sim/rl/__init__.py`
- Create: `provider_sim/rl/network.py`

**Kontext:** Das `rl`-Paket ist neu. Es liegt neben `pdl/`, `sim/`, `env/` im `provider_sim/`-Paket.

**Step 1: `__init__.py` erstellen**

```python
# provider_sim/rl/__init__.py
from __future__ import annotations
```

**Step 2: `network.py` schreiben**

```python
# provider_sim/rl/network.py
"""PPO shared network: MLP backbone + policy head + value head."""
from __future__ import annotations

import torch
import torch.nn as nn
import torch.nn.functional as F
from torch.distributions import Normal


class PPONet(nn.Module):
    """Shared MLP for PPO actor-critic.

    Architecture: Linear(n_obs, 128) -> ReLU -> Linear(128, 64) -> ReLU
                  +-- policy_head: Linear(64, n_act)  [logit means]
                  +-- value_head:  Linear(64, 1)       [state value]

    Exploration via learnable log_std per action dimension.
    Budget constraint: Softmax(sampled_logits) x budget.
    """

    def __init__(self, n_obs: int = 99, n_act: int = 20) -> None:
        super().__init__()
        self.shared = nn.Sequential(
            nn.Linear(n_obs, 128),
            nn.ReLU(),
            nn.Linear(128, 64),
            nn.ReLU(),
        )
        self.policy_head = nn.Linear(64, n_act)
        self.log_std = nn.Parameter(torch.zeros(n_act))
        self.value_head = nn.Linear(64, 1)

    def forward(self, x: torch.Tensor) -> tuple:
        """Forward pass.

        Returns:
            mu:    (batch, n_act) -- policy mean logits
            std:   (batch, n_act) -- policy std (exp of log_std)
            value: (batch,)       -- state value
        """
        hidden = self.shared(x)
        mu = self.policy_head(hidden)
        std = self.log_std.exp().expand_as(mu)
        value = self.value_head(hidden).squeeze(-1)
        return mu, std, value

    def sample_action(self, obs: torch.Tensor, budget: float) -> tuple:
        """Sample action from policy.

        Args:
            obs:    (n_obs,) observation tensor
            budget: total budget to distribute

        Returns:
            actions:        (n_act,) -- Softmax(logits) x budget
            sampled_logits: (n_act,) -- raw sample (stored for PPO update)
            log_prob:       scalar   -- sum of log probs under Normal(mu, std)
            value:          scalar   -- state value estimate
        """
        mu, std, value = self.forward(obs.unsqueeze(0))
        mu, std, value = mu.squeeze(0), std.squeeze(0), value.squeeze(0)
        dist = Normal(mu, std)
        sampled_logits = dist.rsample()
        log_prob = dist.log_prob(sampled_logits).sum()
        weights = F.softmax(sampled_logits, dim=-1)
        actions = weights * budget
        return actions, sampled_logits, log_prob, value

    def recompute_logprob(self, obs: torch.Tensor, sampled_logits: torch.Tensor) -> tuple:
        """Recompute log_prob and value for stored (obs, logits) pairs.

        Args:
            obs:            (batch, n_obs)
            sampled_logits: (batch, n_act)

        Returns:
            log_prob: (batch,)
            value:    (batch,)
            entropy:  scalar
        """
        mu, std, value = self.forward(obs)
        dist = Normal(mu, std)
        log_prob = dist.log_prob(sampled_logits).sum(-1)
        entropy = dist.entropy().sum(-1).mean()
        return log_prob, value, entropy
```

**Step 3: Smoke-Test**

```bash
cd /Users/aschaefer/Projekte/Forschung/PROVIDER/04_Apps/palestrai_simulation
python -c "
import torch
from provider_sim.rl.network import PPONet
net = PPONet(n_obs=99, n_act=20)
obs = torch.rand(99)
actions, logits, log_prob, value = net.sample_action(obs, budget=0.8)
print('actions shape:', actions.shape)
print('actions sum:', round(actions.sum().item(), 6))
assert abs(actions.sum().item() - 0.8) < 1e-5, 'Budget constraint violated!'
print('OK')
"
```

**Step 4: Commit**

```bash
cd /Users/aschaefer/Projekte/Forschung/PROVIDER/04_Apps/palestrai_simulation
git add provider_sim/rl/__init__.py provider_sim/rl/network.py
git commit -m "feat: add PPONet with Gaussian policy and budget constraint"
```

---

### Task 2: `provider_sim/rl/ppo_muscle.py` — PPOMuscle

**Files:**
- Create: `provider_sim/rl/ppo_muscle.py`

**Kontext:** PPOMuscle erbt von palaestrAI `Muscle` ABC. `propose_actions()` gibt ein 4-Tuple zurück: `(env_actions, last_actions, last_inputs, additional_data)`. `additional_data` enthält `sampled_logits`, `log_prob`, `value` für das Brain. `update()` empfängt `state_dict` vom Brain und lädt es in das lokale Netz.

**Step 1: `ppo_muscle.py` schreiben**

```python
# provider_sim/rl/ppo_muscle.py
"""PPO Muscle: inference, budget-constrained action proposal."""
from __future__ import annotations

import numpy as np
import torch

from provider_sim.rl.network import PPONet

try:
    from palaestrai.agent.muscle import Muscle
except ImportError:
    class Muscle:  # type: ignore[no-redef]
        """Stub for environments without palaestrAI."""
        def __init__(self, *args, **kwargs):
            pass


class PPOMuscle(Muscle):
    """PPO Muscle for palaestrAI.

    Params (YAML):
        budget: float -- total action budget (default 0.8 for attacker, 0.4 for defender)
        n_obs:  int   -- observation size (default 99)
        n_act:  int   -- action size (default 20)
    """

    def __init__(self, *args, budget: float = 0.8, n_obs: int = 99, n_act: int = 20, **kwargs) -> None:
        super().__init__(*args, **kwargs)
        self._budget = float(budget)
        self._n_obs = int(n_obs)
        self._n_act = int(n_act)
        self._net: PPONet | None = None

    def setup(self) -> None:
        self._net = PPONet(n_obs=self._n_obs, n_act=self._n_act)
        self._net.eval()

    def propose_actions(self, sensors, actuators_available, is_terminal: bool = False):
        """Propose budget-constrained actions via PPO policy."""
        if self._net is None:
            self.setup()

        obs_list = []
        for s in sensors:
            val = s.sensor_value
            if hasattr(val, '__iter__'):
                obs_list.extend(float(v) for v in np.asarray(val).flatten())
            else:
                obs_list.append(float(val))
        obs = torch.tensor(obs_list, dtype=torch.float32)

        with torch.no_grad():
            actions_t, sampled_logits, log_prob, value = self._net.sample_action(obs, self._budget)

        actions_np = actions_t.numpy()

        for actuator, act_val in zip(actuators_available, actions_np):
            try:
                actuator(float(np.clip(act_val, 0.0, 1.0)))
            except Exception:
                actuator(0.0)

        additional_data = {
            "sampled_logits": sampled_logits.numpy(),
            "log_prob": log_prob.item(),
            "value": value.item(),
        }

        return (actuators_available, actuators_available, obs_list, additional_data)

    def update(self, update) -> None:
        """Load new state_dict from Brain after PPO update."""
        if update is None or self._net is None:
            return
        try:
            self._net.load_state_dict(update)
        except Exception as exc:
            print(f"[PPOMuscle] update failed: {exc}")

    def prepare_model(self) -> None:
        if self._net is not None:
            self._net.eval()

    def __repr__(self) -> str:
        return f"PPOMuscle(budget={self._budget}, n_obs={self._n_obs}, n_act={self._n_act})"

    @property
    def parameters(self) -> dict:
        return {"budget": self._budget, "n_obs": self._n_obs, "n_act": self._n_act}
```

**Step 2: Smoke-Test**

```bash
cd /Users/aschaefer/Projekte/Forschung/PROVIDER/04_Apps/palestrai_simulation
python -c "
import numpy as np, sys; sys.path.insert(0, '.')
from provider_sim.rl.ppo_muscle import PPOMuscle

class FakeSensor:
    def __init__(self, val):
        self.sensor_value = np.array([val], dtype=np.float32)

class FakeActuator:
    def __init__(self): self._val = 0.0
    def __call__(self, v): self._val = v
    @property
    def setpoint(self): return self._val

sensors = [FakeSensor(0.7) for _ in range(99)]
actuators = [FakeActuator() for _ in range(20)]
m = PPOMuscle(budget=0.8)
m.setup()
env_acts, _, _, additional = m.propose_actions(sensors, actuators)
total = sum(a.setpoint for a in env_acts)
print('Actuator sum:', round(total, 6))
assert abs(total - 0.8) < 1e-4, f'Budget violated: {total}'
print('OK')
"
```

**Step 3: Commit**

```bash
cd /Users/aschaefer/Projekte/Forschung/PROVIDER/04_Apps/palestrai_simulation
git add provider_sim/rl/ppo_muscle.py
git commit -m "feat: add PPOMuscle with budget-constrained Softmax policy"
```

---

### Task 3: `provider_sim/rl/ppo_brain.py` — PPOBrain

**Files:**
- Create: `provider_sim/rl/ppo_brain.py`

**Kontext:** PPOBrain erbt von palaestrAI `Brain` ABC. `thinking()` wird nach jedem Step aufgerufen. Brain sammelt Trajectory. Bei `done=True`: GAE-Advantages berechnen, PPO-Update, `state_dict` zurückgeben.

PPO-Hyperparameter (defaults): `lr=3e-4`, `gamma=0.99`, `gae_lambda=0.95`, `clip_eps=0.2`, `ppo_epochs=4`, `value_coef=0.5`, `entropy_coef=0.01`.

**Step 1: `ppo_brain.py` schreiben**

```python
# provider_sim/rl/ppo_brain.py
"""PPO Brain: trajectory accumulation + PPO update."""
from __future__ import annotations

from typing import Any
import numpy as np
import torch
import torch.nn.functional as F
from torch.distributions import Normal

from provider_sim.rl.network import PPONet

try:
    from palaestrai.agent.brain import Brain
    from palaestrai.core.protocol.muscle_update_rsp import MuscleUpdateResponse
except ImportError:
    class Brain:  # type: ignore[no-redef]
        def __init__(self, *args, **kwargs): pass

    class MuscleUpdateResponse:  # type: ignore[no-redef]
        def __init__(self, is_updated: bool, updates: Any):
            self.is_updated = is_updated
            self.updates = updates


class PPOBrain(Brain):
    """PPO Brain for palaestrAI.

    Params (YAML):
        n_obs, n_act, lr, gamma, gae_lambda, clip_eps, ppo_epochs, value_coef, entropy_coef
    """

    def __init__(
        self, *args,
        n_obs: int = 99, n_act: int = 20,
        lr: float = 3e-4, gamma: float = 0.99, gae_lambda: float = 0.95,
        clip_eps: float = 0.2, ppo_epochs: int = 4,
        value_coef: float = 0.5, entropy_coef: float = 0.01,
        **kwargs,
    ) -> None:
        super().__init__(*args, **kwargs)
        self._n_obs = int(n_obs)
        self._n_act = int(n_act)
        self._lr = float(lr)
        self._gamma = float(gamma)
        self._gae_lambda = float(gae_lambda)
        self._clip_eps = float(clip_eps)
        self._ppo_epochs = int(ppo_epochs)
        self._value_coef = float(value_coef)
        self._entropy_coef = float(entropy_coef)

        self._net = PPONet(n_obs=self._n_obs, n_act=self._n_act)
        self._optimizer = torch.optim.Adam(self._net.parameters(), lr=self._lr)

        self._obs_buf: list[np.ndarray] = []
        self._logits_buf: list[np.ndarray] = []
        self._reward_buf: list[float] = []
        self._log_prob_buf: list[float] = []
        self._value_buf: list[float] = []
        self._done_buf: list[bool] = []
        self._episode = 0
        self._total_reward = 0.0

    def thinking(self, muscle_id, readings, actions, reward, next_state, done, additional_data) -> MuscleUpdateResponse:
        # Scalar reward
        if isinstance(reward, (int, float)):
            r = float(reward)
        elif isinstance(reward, list) and len(reward) > 0:
            try:
                r = float(self.objective.internal_reward(reward))
            except Exception:
                r = float(np.mean([float(ri.reward_value) for ri in reward]))
        else:
            r = 0.0

        self._total_reward += r
        obs = self._extract_obs(readings)
        sampled_logits = additional_data.get("sampled_logits", np.zeros(self._n_act))
        log_prob = float(additional_data.get("log_prob", 0.0))
        value = float(additional_data.get("value", 0.0))

        self._obs_buf.append(obs)
        self._logits_buf.append(sampled_logits)
        self._reward_buf.append(r)
        self._log_prob_buf.append(log_prob)
        self._value_buf.append(value)
        self._done_buf.append(bool(done))

        if not done:
            return MuscleUpdateResponse(False, None)

        self._episode += 1
        avg_r = self._total_reward / max(len(self._reward_buf), 1)
        print(
            f"\n[PPOBrain] Episode {self._episode}  "
            f"steps={len(self._reward_buf)}  "
            f"O reward={avg_r:.4f}  "
            f"total={self._total_reward:.4f}"
        )

        state_dict = self._ppo_update(next_value=0.0)

        self._obs_buf.clear(); self._logits_buf.clear()
        self._reward_buf.clear(); self._log_prob_buf.clear()
        self._value_buf.clear(); self._done_buf.clear()
        self._total_reward = 0.0

        return MuscleUpdateResponse(True, state_dict)

    def store_model(self, path: str) -> None:
        torch.save(self._net.state_dict(), path)

    def load_model(self, path: str) -> None:
        self._net.load_state_dict(torch.load(path, weights_only=True))

    def _extract_obs(self, sensors) -> np.ndarray:
        obs_list = []
        for s in sensors:
            val = s.sensor_value
            if hasattr(val, '__iter__'):
                obs_list.extend(float(v) for v in np.asarray(val).flatten())
            else:
                obs_list.append(float(val))
        return np.array(obs_list, dtype=np.float32)

    def _compute_gae(self, next_value: float) -> tuple:
        T = len(self._reward_buf)
        values = self._value_buf + [next_value]
        advantages = np.zeros(T, dtype=np.float32)
        gae = 0.0
        for t in reversed(range(T)):
            delta = (
                self._reward_buf[t]
                + self._gamma * values[t + 1] * (1.0 - float(self._done_buf[t]))
                - values[t]
            )
            gae = delta + self._gamma * self._gae_lambda * (1.0 - float(self._done_buf[t])) * gae
            advantages[t] = gae
        returns = advantages + np.array(self._value_buf, dtype=np.float32)
        adv_t = torch.tensor(advantages)
        adv_t = (adv_t - adv_t.mean()) / (adv_t.std() + 1e-8)
        return adv_t, torch.tensor(returns)

    def _ppo_update(self, next_value: float) -> dict:
        obs_t = torch.tensor(np.stack(self._obs_buf), dtype=torch.float32)
        logits_t = torch.tensor(np.stack(self._logits_buf), dtype=torch.float32)
        old_log_prob_t = torch.tensor(self._log_prob_buf, dtype=torch.float32)
        advantages, returns = self._compute_gae(next_value)

        self._net.train()
        for _ in range(self._ppo_epochs):
            new_log_prob, new_value, entropy = self._net.recompute_logprob(obs_t, logits_t)
            ratio = torch.exp(new_log_prob - old_log_prob_t)
            clipped = torch.clamp(ratio, 1.0 - self._clip_eps, 1.0 + self._clip_eps)
            actor_loss = -torch.min(ratio * advantages, clipped * advantages).mean()
            critic_loss = F.mse_loss(new_value, returns)
            loss = actor_loss + self._value_coef * critic_loss - self._entropy_coef * entropy
            self._optimizer.zero_grad()
            loss.backward()
            torch.nn.utils.clip_grad_norm_(self._net.parameters(), 0.5)
            self._optimizer.step()

        self._net.eval()
        print(
            f"[PPOBrain] PPO update done  "
            f"actor={actor_loss.item():.4f}  "
            f"critic={critic_loss.item():.4f}  "
            f"entropy={entropy.item():.4f}"
        )
        return {k: v.cpu() for k, v in self._net.state_dict().items()}
```

**Step 2: Unit-Test**

```bash
cd /Users/aschaefer/Projekte/Forschung/PROVIDER/04_Apps/palestrai_simulation
python -c "
import numpy as np, sys; sys.path.insert(0, '.')
from provider_sim.rl.ppo_brain import PPOBrain

class FakeSensor:
    def __init__(self, val):
        self.sensor_value = np.array([val], dtype=np.float32)

class FakeObjective:
    def internal_reward(self, rewards): return 0.5

brain = PPOBrain(n_obs=99, n_act=20, ppo_epochs=2)
brain.objective = FakeObjective()
sensors = [FakeSensor(0.7) for _ in range(99)]
additional = {'sampled_logits': np.random.randn(20).astype(np.float32), 'log_prob': -15.0, 'value': 0.5}

for i in range(4):
    resp = brain.thinking('m1', sensors, [], 0.5, sensors, False, additional)
    assert not resp.is_updated

resp = brain.thinking('m1', sensors, [], 0.5, sensors, True, additional)
assert resp.is_updated
assert resp.updates is not None
print('state_dict keys:', len(resp.updates))
print('OK')
"
```

**Step 3: Commit**

```bash
cd /Users/aschaefer/Projekte/Forschung/PROVIDER/04_Apps/palestrai_simulation
git add provider_sim/rl/ppo_brain.py
git commit -m "feat: add PPOBrain with GAE advantages and clipped surrogate loss"
```

---

### Task 4: `experiments/soja_arl_ppo.yaml` — Experiment-Config

**Files:**
- Create: `experiments/soja_arl_ppo.yaml`

**Kontext:** Basiert auf `soja_arl_dummy.yaml`. Ändere Brain/Muscle auf PPO. Attacker: `budget=0.8`, Defender: `budget=0.4`. Episodes: 50.

Kopiere `soja_arl_dummy.yaml` und ändere folgende Felder:

1. `uid`: `provider-soy_feed_disruption-arl-ppo`
2. Attacker `brain.name`: `provider_sim.rl.ppo_brain:PPOBrain`
3. Attacker `brain.params`: `{n_obs: 99, n_act: 20, lr: 0.0003, gamma: 0.99, gae_lambda: 0.95, clip_eps: 0.2, ppo_epochs: 4, value_coef: 0.5, entropy_coef: 0.01}`
4. Attacker `muscle.name`: `provider_sim.rl.ppo_muscle:PPOMuscle`
5. Attacker `muscle.params`: `{budget: 0.8, n_obs: 99, n_act: 20}`
6. Defender analog mit `muscle.params: {budget: 0.4, n_obs: 99, n_act: 20}`
7. `phase_config.episodes`: `50`

**Step 1: YAML generieren**

```bash
cd /Users/aschaefer/Projekte/Forschung/PROVIDER/04_Apps/palestrai_simulation
python -c "
import yaml, copy, pathlib
src = yaml.safe_load(open('experiments/soja_arl_dummy.yaml'))
src['uid'] = 'provider-soy_feed_disruption-arl-ppo'
agents = src['schedule'][0]['phase_train']['agents']
ppo_params = dict(n_obs=99, n_act=20, lr=0.0003, gamma=0.99, gae_lambda=0.95, clip_eps=0.2, ppo_epochs=4, value_coef=0.5, entropy_coef=0.01)
budgets = {'attacker': 0.8, 'defender': 0.4}
for agent in agents:
    name = agent['name']
    agent['brain'] = {'name': 'provider_sim.rl.ppo_brain:PPOBrain', 'params': copy.copy(ppo_params)}
    agent['muscle'] = {'name': 'provider_sim.rl.ppo_muscle:PPOMuscle', 'params': {'budget': budgets[name], 'n_obs': 99, 'n_act': 20}}
src['schedule'][0]['phase_train']['phase_config']['episodes'] = 50
with open('experiments/soja_arl_ppo.yaml', 'w') as f:
    yaml.dump(src, f, default_flow_style=False, allow_unicode=True)
print('Geschrieben: experiments/soja_arl_ppo.yaml')
"
```

**Step 2: Verifikation**

```bash
python -c "
import yaml
data = yaml.safe_load(open('experiments/soja_arl_ppo.yaml'))
agents = data['schedule'][0]['phase_train']['agents']
print('episodes:', data['schedule'][0]['phase_train']['phase_config']['episodes'])
for a in agents:
    print(a['name'], '-> brain:', a['brain']['name'].split(':')[1], '| budget:', a['muscle']['params']['budget'])
"
```

Erwartete Ausgabe:
```
episodes: 50
attacker -> brain: PPOBrain | budget: 0.8
defender -> brain: PPOBrain | budget: 0.4
```

**Step 3: Commit**

```bash
git add experiments/soja_arl_ppo.yaml
git commit -m "feat: add soja_arl_ppo.yaml with PPO brain/muscle, 50 episodes"
```

---

### Task 5: `experiments/plot_training_curves.py` — Trainingskurven

**Files:**
- Create: `experiments/plot_training_curves.py`

**Kontext:** Parst den PPOBrain-Log-Output (via `tee`), extrahiert Reward-Werte pro Episode, erstellt geglättete Kurven.

**Step 1: Script schreiben**

```python
#!/usr/bin/env python3
"""Plot PPO training curves from palaestrAI log output.

Usage:
    palaestrai experiment-start experiments/soja_arl_ppo.yaml 2>&1 | tee experiments/ppo_training.log
    python experiments/plot_training_curves.py
"""
from __future__ import annotations

import argparse
import re
import sys
from pathlib import Path

import matplotlib
matplotlib.use("Agg")
import matplotlib.pyplot as plt
import numpy as np

_LOG_DEFAULT = Path(__file__).resolve().parent / "ppo_training.log"
_OUTPUT_DEFAULT = Path(__file__).resolve().parent / "ppo_curves.png"

# Matches: [PPOBrain] Episode 3  steps=365  O reward=0.3142  total=114.6830
_PATTERN = re.compile(
    r"\[PPOBrain\] Episode\s+(\d+)\s+steps=\d+\s+O reward=([\d.]+)\s+total=([\d.]+)"
)


def parse_log(log_path: str) -> dict:
    attacker: list[float] = []
    defender: list[float] = []
    all_entries: list[float] = []
    with open(log_path) as f:
        for line in f:
            m = _PATTERN.search(line)
            if m:
                all_entries.append(float(m.group(2)))
    for i, avg_r in enumerate(all_entries):
        (attacker if i % 2 == 0 else defender).append(avg_r)
    return {"attacker": attacker, "defender": defender}


def smooth(values: list, window: int = 5) -> np.ndarray:
    if len(values) < window:
        return np.array(values)
    return np.convolve(values, np.ones(window) / window, mode="valid")


def plot_curves(data: dict, output_path: str) -> None:
    fig, axes = plt.subplots(1, 2, figsize=(12, 5))
    for ax, (agent, rewards) in zip(axes, data.items()):
        if not rewards:
            ax.text(0.5, 0.5, f"Keine Daten fuer {agent}", ha="center", va="center")
            continue
        eps = np.arange(1, len(rewards) + 1)
        ax.plot(eps, rewards, alpha=0.3, color="#2196F3", linewidth=1, label="Roh")
        if len(rewards) >= 5:
            sm = smooth(rewards)
            ax.plot(np.arange(3, 3 + len(sm)), sm, color="#2196F3", linewidth=2, label="Geglaettet (w=5)")
        ax.set_xlabel("Episode", fontsize=11)
        ax.set_ylabel("O Reward pro Tick", fontsize=11)
        ax.set_title(f"{agent.capitalize()} - PPO Trainingskurve", fontsize=12)
        ax.legend(fontsize=9)
        ax.grid(axis="y", linestyle="--", alpha=0.4)
    plt.suptitle("PPO Adversarial RL - Soja S1  (Attack=0.8, Defend=0.4, 50 Ep x 365 Ticks)", fontsize=12)
    plt.tight_layout()
    plt.savefig(output_path, dpi=150, bbox_inches="tight")
    plt.close(fig)
    print(f"  Grafik gespeichert: {output_path}")


def parse_args() -> argparse.Namespace:
    p = argparse.ArgumentParser()
    p.add_argument("--log", default=str(_LOG_DEFAULT))
    p.add_argument("--output", default=str(_OUTPUT_DEFAULT))
    return p.parse_args()


if __name__ == "__main__":
    args = parse_args()
    if not Path(args.log).exists():
        print(f"Log nicht gefunden: {args.log}")
        print("Experiment starten: palaestrai experiment-start experiments/soja_arl_ppo.yaml 2>&1 | tee experiments/ppo_training.log")
        sys.exit(1)
    data = parse_log(args.log)
    print(f"Attacker: {len(data['attacker'])} Episoden, Defender: {len(data['defender'])} Episoden")
    plot_curves(data, args.output)
```

**Step 2: Syntax-Check**

```bash
python -m py_compile experiments/plot_training_curves.py && echo "Syntax OK"
```

**Step 3: Commit**

```bash
git add experiments/plot_training_curves.py
git commit -m "feat: add plot_training_curves.py for PPO reward visualization"
```

---

### Task 6: Integrations-Verifikation (3 Episoden)

**Files:** Keine Code-Änderungen.

**Step 1: Ports prüfen**

```bash
lsof -i :4242 -i :4243 2>/dev/null | grep LISTEN || echo "Ports frei"
```

**Step 2: Test-Run mit 3 Episoden**

```bash
cd /Users/aschaefer/Projekte/Forschung/PROVIDER/04_Apps/palestrai_simulation
sed 's/episodes: 50/episodes: 3/' experiments/soja_arl_ppo.yaml > /tmp/soja_ppo_3ep.yaml
palaestrai experiment-start /tmp/soja_ppo_3ep.yaml 2>&1 | tee /tmp/ppo_test.log
```

**Erwartete Log-Ausgaben:**
```
[PPOBrain] Episode 1  steps=365  O reward=...  total=...
[PPOBrain] PPO update done  actor=...  critic=...  entropy=...
```

**Verifikation:**

```bash
grep "\[PPOBrain\] Episode" /tmp/ppo_test.log | wc -l
# Erwartet: 6 (3 Episoden x 2 Agenten)
```

Falls Fehler: Bug in `ppo_brain.py` oder `ppo_muscle.py` finden und fixen.

---

### Task 7 (optional): 50-Episoden-Lauf + Kurven

**Step 1:**

```bash
cd /Users/aschaefer/Projekte/Forschung/PROVIDER/04_Apps/palestrai_simulation
palaestrai experiment-start experiments/soja_arl_ppo.yaml 2>&1 | tee experiments/ppo_training.log
```

**Step 2:**

```bash
python experiments/plot_training_curves.py
```

**Step 3:**

```bash
git add experiments/ppo_training.log experiments/ppo_curves.png
git commit -m "feat: PPO adversarial RL - 50-episode training results (Soja S1)"
```
